class Solution:
    def getMinMax(self, arr):
        # code here
        return [min(arr),max(arr)]

#Logic: We can use the built-in min() and max() functions to find the minimum and maximum elements in the array. 
#Time Complexity: O(n)
#Space Complexity: O(1)